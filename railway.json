{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "apt-get update && apt-get install -y python3 make g++ && cd server && npm install",
    "nixpacksConfigPath": "nixpacks.toml"
  },
  "deploy": {
    "startCommand": "cd server && NODE_ENV=production SERVER_ONLY=true node server.js",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 60
  }
}